{
  prep: npm install
}

**/*.go **/*.templ **/*.css **/*.sql !**/*_test.go !**/*_templ.go !**/node_modules/* !**/public/* !**/stores/* {
  prep: templ generate
  prep: npm run dev
  prep: sqlc generate
  daemon +sigterm: go run .
}
